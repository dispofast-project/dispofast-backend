spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/dispofast
    username: dispofast
    password: dispofast
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    validate-on-migrate: false
  mail:
    host: business40.web-hosting.com
    port: 995
    username: sistemassl@dispocol.com
    password: password123
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  jwt:
    secret: ${JWT_SECRET:ihavetochangeThisSecretKeysoon1234567890}
    expirationSeconds: 3600000
  minio:
    endpoint: http://localhost:9000
    access-key: admin
    secret-key: password123
  
  # Remitente por defecto para inyectar en MailServiceImpl
  mail:
    from: sistemassl@dispocol.com

logging:
  level:
    com.dispocol.dispofast.modules.iam.infra.security: DEBUG
